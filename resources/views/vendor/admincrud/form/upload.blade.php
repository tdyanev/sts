<div class="form-group">
    <label for="input-{{ $name }}">{{ $label }}</label>
    <input type="file" id="input-{{ $name }}" name="{{ $name }}" />
    <p class="help-block">Example block-level help text here.</p>
</div>